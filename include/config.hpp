#ifndef CONFIG
#define CONFIG

/* WINDOW */
const unsigned short WINDOW_WIDTH = 800;
const unsigned short WINDOW_HEIGHT = 600;

/* AGENT */
const unsigned int AGENT_COUNT = 1024;
const float AGENT_SPEED = 0.00001f;

/* AGENT - SENSOR */
const unsigned short SENSOR_SIZE = 1;
const float SENSOR_DISTANCE = 3.0f;
const float SENSOR_ANGLE = 1.0f;

/* SHADERS */
const float BLUR_SPEED = 0.8f;
const float FADE_SPEED = 0.001f;

#endif